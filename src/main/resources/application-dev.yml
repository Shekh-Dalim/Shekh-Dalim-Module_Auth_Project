#development version of configuration

server:
  port: 8083

#  Database Connection

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_app_yt
    username: postgres
    password: Ruhul@2002

    #    hikari:
    #      pool-name: HikariCP              # Connection pool name
    #      maximum-pool-size: 5             # Maximum 5 DB connections
    #      connection-timeout: 10000        # Wait 10 sec for connection
    #      idle-timeout: 600000             # Remove idle connection after 10 min
    #      max-lifetime: 1800000            # Maximum connection lifetime: 30 min
    #      validation-timeout: 1800000      # Timeout for connection validation
    #      leak-detection-threshold: 0      # Disable connection leak detection
    #      initialization-fail-timeout: -1  # Don't fail startup if DB is unavailable

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # Modern Hibernate/Spring Boot automatically detects the database dialect from the JDBC connection, so we usually don't need to specify it manually.
        format_sql: true
    show-sql: true  # means Spring Boot will display the SQL queries executed by Hibernate in the console.


# this line written to visual the error of the code in console
logging:
  level:
    org.springframework.security: DEBUG


#  TODO application.yml stores the JWT configuration,
security:
  jwt:
     secret: ${JWT_SECRET:saiofal;vkasfbehwqpofhjashofjaswfpojasfvbafop[hfifonfo[shjfwefjjfslajfsahfoshfffasnbash[f} # TODO JWT needs a secret key to create and verify tokens.
     issuer: ${JWT_ISSUER:api.substring.com}                            #TODO I will write who created (issued) this token.
     access-ttl-seconds: ${JWT_ACCESS_TTL_SECONDS:3600}                 #TODO his Access Token will be valid for 1 hour.
     refresh-ttl-seconds: ${JWT_REFRESH_TTL_SECONDS:86400}              #TODO his Refresh Token will be valid for 24 hours.
     refresh-token-cookie-name: ${JWT_REFRESH_COOKIE_NAME:refreshToken} #TODO Save the Refresh Token in the browser using the cookie name "refreshToken".
     cookie-secure: ${JWT_COOKIE_SECURE:true}                           #TODO Send this cookie only over HTTPS.
     cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}                     #TODO Do not allow JavaScript to read this cookie.
     cookie-same-site: ${JWT_COOKIE_SAME_SITE:lax}                      #TODO TODO Do not send this cookie with suspicious cross-site requests.
     cookie-domain: ${JWT_COOKIE_DOMAIN:localhost}

